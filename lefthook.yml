pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check -- {staged_files}
    clippy:
      glob: "*.rs"
      run: cargo clippy --all-targets -- -D warnings
